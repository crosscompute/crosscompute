{% extends BASE_JINJA2 %}

{% block body_js %}
let mutationTimestamp = {{ mutation_timestamp }};
/*
setInterval(async function() {
  let response = await fetch('{{ BASE_URI }}{{ mutation_uri }}');
  const d = await response.json();
  if (serverTimestamp === d.server_timestamp) return;
  response = await fetch(location.href, { method: 'head' });
  if (response.ok) {
    location.reload();
  } else {
    location.href = '/';
  }
}, {{ PING_INTERVAL_IN_MILLISECONDS }});
*/
{{ super() -}}
{%- endblock %}
